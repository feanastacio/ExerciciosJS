// Crie uma função que gera uma senha de 10 caracteres com letras, números e símbolos
// usando Math.random(), charCodeAt() e String.fromCharCode().
let qntd = 10;
function combinacao(qntd) {
    let senhaaleatoria = [];
    for (let i = 0; i <= qntd; i++) {
        let num = Math.random();        
    }

}